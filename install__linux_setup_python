# install python
sudo apt-get update
sudo apt-get install python3.6

sudo apt-get install python3-distutils
sudo apt-get install python3-apt

sudo apt-get install software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt-get update
sudo apt-get install python3.8


# install pip in python3
sudo apt install python3-pip

# venv setups
python3 -m pip install --user --upgrade pip
python3 -m pip --version

python3 -m pip install --user virtualenv
OR
sudo apt install python3.8-venv

# create venv
python3 -m venv env

source env/bin/activate

pip3 install numpy pandas matplotlib opencv-python scikit-learn scikit-image sklearn tqdm click setuptools h5py nltk opencv-contrib-python face_recognition pillow pdf2image pyodbc flask requests scipy tensorflow click dlib pyodbc

